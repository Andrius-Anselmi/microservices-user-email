spring:
  application:
    name: email

  datasource:
    url: ${EMAIL_DB_URL:jdbc:postgresql://localhost:5433/ms-email-ms}
    username: ${EMAIL_DB_USERNAME:postgres}
    password: ${EMAIL_DB_PASSWORD:postgres}
    driver-class-name: ${EMAIL_DB_DRIVER:org.postgresql.Driver}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: ${EMAIL_RABBIT_HOST:chameleon.lmq.cloudamqp.com}
    username: ${EMAIL_RABBIT_USERNAME:qknjvqyn}
    password: ${EMAIL_RABBIT_PASSWORD:GJ3aHWASwX1FreWIkxuYJBjUsJmuSzo4}
    virtual-host: ${EMAIL_RABBIT_VHOST:qknjvqyn}

server:
  port: ${EMAIL_SERVER_PORT:8080}


